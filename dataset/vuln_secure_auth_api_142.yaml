api_name: Secure Auth API
description: 'Provides safe authentication and token-based access control. Keywords:
  sanitize, parameterized_query, secure, token Keywords: validate, jwt, secure, rate_limited
  Keywords: auth_required, jwt, sanitize, rate_limited Keywords: auth_required, sanitize,
  jwt, token Keywords: encrypt, sanitize, token, jwt Keywords: token, auth_required,
  csrf_protected, sanitize Keywords: jwt, validate, input_filter, secure Keywords:
  validate, encrypt, sanitize, parameterized_query Keywords: token, parameterized_query,
  jwt, validate Keywords: jwt, token, rate_limited, encrypt Keywords: parameterized_query,
  encrypt, rate_limited, auth_required Keywords: rate_limited, encrypt, csrf_protected,
  parameterized_query Keywords: secure, encrypt, parameterized_query, token Keywords:
  jwt, parameterized_query, secure, validate Keywords: secure, encrypt, parameterized_query,
  jwt Keywords: parameterized_query, jwt, rate_limited, csrf_protected Keywords: csrf_protected,
  jwt, sanitize, parameterized_query Keywords: csrf_protected, jwt, token, encrypt
  Keywords: auth_required, input_filter, rate_limited, encrypt Keywords: jwt, csrf_protected,
  secure, rate_limited Keywords: parameterized_query, sanitize, auth_required, input_filter
  Keywords: encrypt, secure, rate_limited, token Mixed endpoints detected: potential
  misconfiguration. Keywords: hardcoded, unvalidated, bypass, debug_enabled'
paths:
  /login:
    post:
      summary: Validates credentials securely
  /token:
    get:
      summary: Returns encrypted JWT access token
  /comment:
    post:
      summary: Echoes user input without sanitization
  /search:
    get:
      summary: Concatenates query strings from parameters
keywords:
- hardcoded
- unvalidated
- bypass
- debug_enabled
