api_name: XSS-prone Comment API
description: 'Displays unfiltered HTML comments in responses. Keywords: debug_enabled,
  leak, bypass, plaintext Keywords: unsafe_query, injection, exposed, leak Keywords:
  bypass, exposed, debug_enabled, xss Keywords: plaintext, injection, bypass, debug_enabled
  Keywords: unsafe_query, bypass, xss, plaintext Keywords: injection, plaintext, hardcoded,
  exposed Keywords: exposed, bypass, missing_auth, hardcoded Keywords: exposed, missing_auth,
  xss, debug_enabled Keywords: missing_auth, xss, hardcoded, injection Keywords: unvalidated,
  leak, bypass, missing_auth Keywords: exposed, injection, hardcoded, debug_enabled'
paths:
  /comment:
    post:
      summary: Echoes user input without sanitization
keywords:
- exposed
- injection
- hardcoded
- debug_enabled
