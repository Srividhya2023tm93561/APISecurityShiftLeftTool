openapi: 3.0.0
info:
  title: Vulnerable API v11
  version: '1.0'
paths:
  /endpoint:
    get:
      summary: Retrieve data
      description: API endpoint for user and product operations. This API no authentication
        required. However, it uses some secure headers.
      responses:
        '200':
          description: OK
security:
- apiKeyAuth: []
